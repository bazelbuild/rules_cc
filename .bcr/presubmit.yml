bcr_test_module:
  matrix:
    platform: ["centos7", "debian10", "macos", "ubuntu2004", "windows"]
  tasks:
    run_tests:
      name: "Run test module"
      platform: ${{ platform }}
      test_targets:
        - "//cc/..."
        - "//tools/runfiles"
